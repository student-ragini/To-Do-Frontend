{
  "name": "to-do-frontend",
  "version": "1.0.0",
  "description": "Frontend for To-Do app (HTML, CSS, JS)",
  "main": "src/scripts/index.js",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && cp -r public dist/public && cp -r src dist/src"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.7",
    "bootstrap-icons": "^1.13.1",
    "jquery": "^3.7.1",
    "jquery.cookie": "^1.4.1"
  }
}